# Database Configuration (PostgreSQL)
# For Railway: Get these from your PostgreSQL service's "Connect" tab
DB_HOST=your-railway-db-host.railway.app
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your-database-password
DB_NAME=railway

# Application
PORT=3000

# JWT Secret (generate a random string)
JWT_SECRET=your-super-secret-jwt-key-change-this

# Email Configuration (for OTP)
# Option 1: Use Ethereal (for testing)
MAIL_HOST=smtp.ethereal.email
MAIL_USER=your-ethereal-username
MAIL_PASS=your-ethereal-password
MAIL_FROM=noreply@foodzy.com

# Option 2: Use Gmail (for production)
# MAIL_HOST=smtp.gmail.com
# MAIL_USER=your-email@gmail.com
# MAIL_PASS=your-app-specific-password
# MAIL_FROM=your-email@gmail.com
